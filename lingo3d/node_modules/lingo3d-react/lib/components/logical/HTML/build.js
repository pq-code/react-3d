import { forceGet } from "@lincode/utils";
import { nanoid } from "nanoid";
import React from "react";
import HTMLChild from "./HTMLChild";
export const elements = [];
const managerIdMap = new WeakMap();
export const build = () => React.createElement(React.Fragment, null, elements.map(([el, manager]) => React.createElement(HTMLChild, { key: forceGet(managerIdMap, manager, nanoid), el: el, manager: manager })));
