import { PerspectiveCamera } from "three";
import EventLoopItem from "../../../api/core/EventLoopItem";
import ICameraMixin from "../../../interface/ICameraMixin";
export default abstract class CameraMixin<T extends PerspectiveCamera> extends EventLoopItem implements ICameraMixin {
    protected abstract camera: T;
    abstract minPolarAngle: number;
    abstract maxPolarAngle: number;
    protected initCamera(this: EventLoopItem & CameraMixin<T>): void;
    get fov(): number;
    set fov(val: number);
    get zoom(): number;
    set zoom(val: number);
    get near(): number;
    set near(val: number);
    get far(): number;
    set far(val: number);
    activate(): void;
    get active(): boolean;
    set active(val: boolean);
    get bokeh(): boolean;
    set bokeh(val: boolean);
    get bokehFocus(): number;
    set bokehFocus(val: number);
    get bokehMaxBlur(): number;
    set bokehMaxBlur(val: number);
    get bokehAperture(): number;
    set bokehAperture(val: number);
}
