import { Group, PerspectiveCamera } from "three";
import ObjectManager from "../ObjectManager";
import CameraMixin from "../mixins/CameraMixin";
import Point3d from "../../../api/Point3d";
import pillShape from "../SimpleObjectManager/PhysicsItem/cannon/shapes/pillShape";
import ICameraBase, { MouseControl, MouseControlMode } from "../../../interface/ICameraBase";
import { Reactive } from "@lincode/reactivity";
import PositionedItem from "../../../api/core/PositionedItem";
declare abstract class CameraBase<T extends PerspectiveCamera> extends ObjectManager<Group> implements ICameraBase {
    protected camera: T;
    protected _physicsShape: typeof pillShape;
    constructor(camera: T);
    protected getRay(): import("three").Ray;
    append(object: PositionedItem): void;
    attach(object: PositionedItem): void;
    get width(): number;
    set width(val: number);
    get height(): number;
    set height(val: number);
    get depth(): number;
    set depth(val: number);
    lookAt(target: PositionedItem | Point3d): void;
    private _gyrate;
    gyrate(movementX: number, movementY: number): void;
    protected updatePolarAngle: () => void | undefined;
    private _minPolarAngle;
    get minPolarAngle(): number;
    set minPolarAngle(val: number);
    private _maxPolarAngle;
    get maxPolarAngle(): number;
    set maxPolarAngle(val: number);
    mouseControlMode?: MouseControlMode;
    protected mouseControlState: Reactive<MouseControl>;
    private mouseControlInit?;
    get mouseControl(): MouseControl;
    set mouseControl(val: MouseControl);
    getCenter(): Point3d;
}
interface CameraBase<T extends PerspectiveCamera> extends ObjectManager<Group>, CameraMixin<T> {
}
export default CameraBase;
