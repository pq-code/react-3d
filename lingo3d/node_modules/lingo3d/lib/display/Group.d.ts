import { Group as ThreeGroup } from "three";
import PositionedItem from "../api/core/PositionedItem";
import IGroup from "../interface/IGroup";
import ObjectManager from "./core/ObjectManager";
export default class Group extends ObjectManager<ThreeGroup> implements IGroup {
    static componentName: string;
    static defaults: IGroup;
    static schema: Required<import("../interface/utils/extractProps").ExtractProps<IGroup>>;
    constructor();
    append(target: PositionedItem): void;
    attach(target: PositionedItem): void;
}
