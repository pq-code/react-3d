import { Group } from "three";
import ObjectManager from "../core/ObjectManager";
import IBoxLight from "../../interface/IBoxLight";
export default class BoxLight extends ObjectManager<Group> implements IBoxLight {
    static componentName: string;
    static defaults: IBoxLight;
    static schema: Required<import("../../interface/utils/extractProps").ExtractProps<IBoxLight>>;
    private makeLight;
    constructor();
    private intensityState;
    get intensity(): number;
    set intensity(val: number);
    private helperState;
    get helper(): boolean;
    set helper(val: boolean);
    private colorState;
    get color(): string;
    set color(val: string);
    private areaState;
    get area(): number;
    set area(val: number);
    getCenter(): import("../..").Point3d;
}
