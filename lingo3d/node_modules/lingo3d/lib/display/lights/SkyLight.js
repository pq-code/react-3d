import { Color, HemisphereLight, HemisphereLightHelper } from "three";
import LightBase from "../core/LightBase";
import { skyLightDefaults, skyLightSchema } from "../../interface/ISkyLight";
export default class default_1 extends LightBase {
    constructor() {
        super(new HemisphereLight(), HemisphereLightHelper);
    }
    get groundColor() {
        return "#" + this.object3d.groundColor.getHexString();
    }
    set groundColor(val) {
        this.object3d.groundColor = new Color(val);
    }
}
default_1.componentName = "skyLight";
default_1.defaults = skyLightDefaults;
default_1.schema = skyLightSchema;
