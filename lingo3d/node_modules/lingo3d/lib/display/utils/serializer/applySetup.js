import { debounce } from "@lincode/utils";
import settings from "../../../api/settings";
import { emitApplySetup } from "../../../events/onApplySetup";
import { setupDefaults } from "../../../interface/ISetup";
export default debounce((node) => {
    var _a;
    for (const key of Object.keys(settings))
        //@ts-ignore
        settings[key] = (_a = node[key]) !== null && _a !== void 0 ? _a : setupDefaults[key];
    emitApplySetup();
}, 0, "trailing");
