import { Group } from "three";
import { getSkyboxStack, pullSkyboxStack, pushSkyboxStack, setSkyboxStack } from "../states/useSkyboxStack";
import { skyboxDefaults, skyboxSchema } from "../interface/ISkybox";
import EventLoopItem from "../api/core/EventLoopItem";
export default class Skybox extends EventLoopItem {
    constructor() {
        super(new Group());
        pushSkyboxStack(this);
    }
    dispose() {
        super.dispose();
        pullSkyboxStack(this);
        return this;
    }
    get texture() {
        return this._texture;
    }
    set texture(value) {
        this._texture = value;
        setSkyboxStack([...getSkyboxStack()]);
    }
}
Skybox.componentName = "skybox";
Skybox.defaults = skyboxDefaults;
Skybox.schema = skyboxSchema;
