import { cameraDefaults, cameraSchema } from "./ICamera";
export const characterCameraSchema = {
    ...cameraSchema,
    target: Object,
    lockTargetRotation: [Boolean, String]
};
export const characterCameraDefaults = {
    ...cameraDefaults,
    lockTargetRotation: true
};
